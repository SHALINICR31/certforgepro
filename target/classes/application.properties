# ─── Server ──────────────────────────────────────────────────────────────────
server.port=8080

# ─── MongoDB Atlas ────────────────────────────────────────────────────────────
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://certforgeuser:CertForge2024@cluster0.9h40ics.mongodb.net/certforge?retryWrites=true&w=majority}
spring.data.mongodb.database=certforge

# ─── JWT ─────────────────────────────────────────────────────────────────────
app.jwt.secret=${JWT_SECRET:CertForgeSecretKey2024SuperSecureKeyForJWT32Chars!}
app.jwt.expiration-ms=28800000

# ─── Email via Resend (free, no credit card, 3000/month) ─────────────────────
# Render blocks SMTP (ports 25/465/587) on free tier — Resend uses HTTPS, always works
resend.api.key=${RESEND_API_KEY:re_ZDVqqWRL_5LuKQ5J9nRnxPCjBehpBFojb}
resend.from.email=${RESEND_FROM_EMAIL:onboarding@resend.dev}
resend.from.name=${RESEND_FROM_NAME:CertForge Pro}

# ─── Upload limits ────────────────────────────────────────────────────────────
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=25MB
server.tomcat.max-http-form-post-size=25MB
server.tomcat.max-swallow-size=25MB
spring.mvc.async.request-timeout=60000

# ─── Hide error details in production ────────────────────────────────────────
server.error.include-stacktrace=never
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-exception=false

# ─── Logging ──────────────────────────────────────────────────────────────────
logging.level.com.certforge=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
